# 2026-04-17 交易计划运行记录

日期：`2026-04-16`
计划目标日：`2026-04-17`

## 本轮运行目标

在健康 worktree 中继续跑 next-session 交易计划，优先验证三件事：

1. `clean repo worktree` 是否可承接 shortlist runtime
2. 明天计划是否仍然会因为过滤面过窄而“全 blocked”
3. 放开核心创业板白名单和叠加 macro-health 后，是否能形成更有用的观察名单

## 运行前修复

worktree 初始状态缺少本地 `.pyc` 运行产物：

- `financial-analysis/skills/short-horizon-shortlist/scripts/__pycache__/month_end_shortlist.cpython-312.pyc`
- `financial-analysis/skills/short-horizon-shortlist/scripts/__pycache__/month_end_shortlist_runtime.cpython-312.pyc`

根因：

- 这两个文件是本地主工作树里的未跟踪运行依赖
- `.gitignore` 会让 worktree 不继承它们

处理：

- 只把这两个本地运行依赖复制进 isolated worktree
- 不纳入版本控制

之后 focused regression 恢复为：

- `23 passed`

## 运行 1：base next-session shortlist

请求：

- `request.base.json`

结果要点：

- `Universe considered = 11`
- `Top picks = 0`
- 全部为 `不执行`

主要问题：

1. `chinext_not_whitelisted` 太多
2. `star_board_excluded` 太多
3. 主板剩余票里大量命中：
   - `rs90_below_threshold`
   - `no_structured_catalyst_within_window`

结论：

- 这不是链路坏掉，而是过滤面太窄
- 如果继续只跑默认主板 strict 口径，很容易 miss 掉真正还值得观察的名字

## 运行 2：放开核心创业板白名单

请求：

- `request.chinext.json`

关键调整：

- 保持 `main_board_only`
- 加入一组核心 `chinext_whitelist`
- 去掉 `exclude_star_board = true`
- 保留当前已跟踪 focus 名单

结果要点：

- `Universe considered = 19`
- `Top picks = 0`
- 但已经形成一组更有价值的 `继续观察 / watch / near_miss` 名单

### 当前最值得保留到次日观察窗口的 near-miss

- `000988.SZ` 华工科技
  - `score = 58.0`
  - `gap = -12.0`
  - 当前没有硬伤，但还没过 keep line

- `002384.SZ` 东山精密
  - `score = 57.0`
  - `gap = -13.0`
  - 趋势和 RS 还在，属于标准 near-miss

- `002463.SZ` 沪电股份
  - `score = 57.0`
  - `gap = -13.0`
  - 结构未坏，但距离执行线仍差一截

- `300857.SZ` 协创数据
  - `score = 56.0`
  - `gap = -14.0`
  - 更像“待确认”而不是“直接执行”

### 当前 watch 但离执行线更远的票

- `300750.SZ` 宁德时代
  - `score = 41.3`
  - `gap = -28.7`
  - 没有硬过滤失败，但离执行线仍远

- `300308.SZ` 中际旭创
  - `score = 41.3`
  - `gap = -28.7`
  - 同样更像结构保留，而不是明天可直接执行

### 当前明确被事件层挡住的票

- `300502.SZ` 新易盛
- `300394.SZ` 天孚通信
- `001309.SZ` 德明利
- `002460.SZ` 赣锋锂业
- `002475.SZ` 立讯精密

这些票共同特征是：

- 图形或结构不一定最差
- 但 `no_structured_catalyst_within_window` 明显压制了执行资格

## 运行 3：macro-health assisted next-session shortlist

请求：

- `request.chinext.json + macro_health_overlay`

macro overlay 结论：

- `health_label = favorable_risk_window`
- `risk_posture = constructive_risk_on`
- `window_state = open`
- takeaway:
  - `Risk assets have a cleaner tradable window, but stock-level setup quality still matters more than macro optimism.`

shortlist 结论：

- 与运行 2 的 shortlist 结论基本一致
- 说明当前主限制不是宏观风险窗口，而是：
  - 个股 keep score 不足
  - 或结构化催化事件不足

## 当前结论

对于 `2026-04-17` 这轮 next-session 计划，当前最有价值的结论不是“已经有可执行 top picks”，而是：

1. 默认 strict 主板口径太容易 miss
2. 加入 bounded `chinext_whitelist` 后，明天计划开始出现可跟踪的 near-miss
3. 修复 `filter_profile` 透传后，`month_end_event_support_transition` 已在真实 CLI 路径生效
4. 当前最值得盯的是：
   - `000988.SZ` 华工科技
     - 已从 near-miss 升到 `qualified / 可执行`
     - `score = 58.0`
     - `gap = 0.0`
   - `002384.SZ` 东山精密
   - `002463.SZ` 沪电股份
   - `300857.SZ` 协创数据
5. 当前最明确的主阻塞不再是链路错误，而是：
   - `score_below_keep_threshold`
   - `no_structured_catalyst_within_window`

## 透传修复

修复前：

- `filter_profile = month_end_event_support_transition`
- CLI 和 compiled runtime 内部都会把它重置成 `month_end_minervini_kell_strict`

修复后：

- wrapper `normalize_request` 保留 `event_support_transition`
- wrapper 在调用 compiled runtime 期间，临时替换 compiled 内部的 `normalize_request`
- CLI shim 也改为走 wrapper runtime，而不是直接交给 compiled `main()`

当前真实输出已经确认：

- `request.filter_profile = month_end_event_support_transition`
- `filter_summary.keep_threshold = 58.0`
- `filter_summary.strict_top_pick_threshold = 59.0`
- `000988.SZ` 华工科技 -> `qualified / 可执行`

因此这个问题现在已经从“待排查”转成“已修复”。

## 运行产物

- base:
  - `.tmp/next-session-2026-04-17/request.base.json`
  - `.tmp/next-session-2026-04-17/result.base.json`
  - `.tmp/next-session-2026-04-17/report.base.md`
- chinext-bounded:
  - `.tmp/next-session-2026-04-17/request.chinext.json`
  - `.tmp/next-session-2026-04-17/result.chinext.json`
  - `.tmp/next-session-2026-04-17/report.chinext.md`
- macro-assisted:
  - `.tmp/next-session-2026-04-17/request.macro.resolved.json`
  - `.tmp/next-session-2026-04-17/result.macro.json`
  - `.tmp/next-session-2026-04-17/report.macro.md`
  - `.tmp/next-session-2026-04-17/overlay.macro.json`

修复后复跑：

- CLI fixed:
  - `.tmp/next-session-2026-04-17/result.chinext.fixed3.json`
  - `.tmp/next-session-2026-04-17/report.chinext.fixed3.md`
- macro-assisted fixed:
  - `.tmp/next-session-2026-04-17/request.macro.fixed.resolved.json`
  - `.tmp/next-session-2026-04-17/result.macro.fixed.json`
  - `.tmp/next-session-2026-04-17/report.macro.fixed.md`
  - `.tmp/next-session-2026-04-17/overlay.macro.fixed.json`

## Discovery Lane Phase 1

本轮又新增了一条 wrapper/orchestration 层的 `earnings-momentum discovery lane`：

1. 允许 `event_discovery_candidates` 直接反向注入交易计划
2. 对传闻类输入给出：
   - `rumor_confidence_range`
   - `market_validation_summary`
3. 输出新增：
   - `直接可执行`
   - `重点观察`
   - `链条跟踪`
   - `Event Board`
   - `Chain Map`

当前 example 验证已经通过：

- request:
  - `financial-analysis/skills/month-end-shortlist/examples/month-end-shortlist-earnings-momentum.template.json`
- output:
  - `.tmp/earnings-momentum-example/result.json`
  - `.tmp/earnings-momentum-example/report.md`

这次 example 中：

- `000988.SZ` 华工科技
  - 通过 `quarterly_preview`
  - discovery lane 直接进入 `直接可执行`
  - markdown 中已真实出现：
    - `## 直接可执行`
    - `## Event Board`
    - `## Chain Map`

## Discovery Lane Phase 1.5

又继续往前推了一层：

1. 不再只依赖手工 `event_discovery_candidates`
2. 现在会从 shortlist 已经拿到的内容里自动抽 discovery candidates：
   - `structured_catalyst_snapshot`
   - `performance_preview`
   - `structured_company_events`

这意味着：

- 没有手工喂事件时
- system 也能从已有 shortlist 评估结果中抽出
  `quarterly_preview / structured_catalyst / company_event` 一类 discovery 信号

当前实跑结果已经验证：

- `.tmp/next-session-2026-04-17/result.chinext.auto-discovery.json`
- `.tmp/next-session-2026-04-17/report.chinext.auto-discovery.md`

在这次自动抽取 run 里，report 已真实出现：

- `## 直接可执行`
- `## 重点观察`
- `## Event Board`
- `## Chain Map`

说明 discovery lane 已经开始具备“从 shortlist 自己长出事件信号”的能力，而不再只是静态手工输入层。

## Discovery Lane Phase 1.6

又继续接入了一层 `x-stock-picker-style` 结果：

1. 同一个 `x_style_batch_result_path`
   - 仍然可以继续生成 shortlist overlay
   - 现在也会额外生成 discovery candidates

2. 当前已支持从 X 风格结果里的这些结构转换：
   - `recommendation_ledger`
   - `direct_pick`
   - `theme_basket`
   - `logic_support`
   - `quote_only`

3. 这意味着：
   - 老法师 / 拖拉机 / Ariston 这类 allowlisted X 账号
   - 只要先被整理进 `x-stock-picker-style` batch result
   - 现在就能顺着同一条文件输入，进入 discovery lane

当前验证面已扩到：

- `test_x_style_assisted_shortlist.py`
- `test_earnings_momentum_discovery.py`
- `test_month_end_shortlist_discovery_merge.py`

focused regression 当前为：

- `40 passed`

## Discovery Lane Phase 1.7

继续往前推了一层：

1. 不再要求一定先跑完整 `x-stock-picker-style` batch
2. 现在 shortlist request 还可以直接带：
   - `x_discovery_request`
   - 里面可放：
     - `subject.handle`
     - `source_board_seed`
     - `x_index_result`
     - `timeline_scan`

3. runtime 会直接复用 `x-stock-picker-style` 的解析逻辑，把这些原始/摘要型 X 输入转成 discovery candidates

4. 如果 `recommendation_ledger` 里没有 ticker，
   现在也会尝试从原文里回填类似：
   - `东山精密(002384)`
   - `002384.SZ`

这意味着：

- 你后面给的这类 X 链接/摘要
- 不一定要先完整跑成 batch
- 也可以先进入 `x_discovery_request` 这条更直接的入口

当前 focused regression 已扩到：

- `42 passed`

## Discovery Lane Phase 1.8

又新增了一层 `传闻 -> 公司回应 -> 升降级` 的状态机：

1. 当前已区分：
   - `official_confirmed`
   - `response_confirmed`
   - `response_ambiguous`
   - `response_denied`
   - `rumor_unconfirmed`

2. 对应 bucket 现在不是静态的：
   - `rumor_unconfirmed` 默认留在 `重点观察`
   - `response_confirmed` 在强验证下可以升级
   - `response_denied` 会降级到 `链条跟踪`

3. report 里现在会明确写出：
   - `事件状态`
   - `交易可用性`

4. direct X 输入的实际验证样例也已经跑过：
   - `.tmp/x-discovery-response-example/result.json`
   - `.tmp/x-discovery-response-example/report.md`

当前 focused regression 已扩到：

- `46 passed`

## Discovery Lane Phase 1.9

又继续补了一层“更方便直接使用”的入口：

1. 现在除了内联 `x_discovery_request`
2. 也支持：
   - `x_discovery_request_path`

这意味着：

- 你可以把正在看的 X 链接内容先整理进一个 JSON 文件
- 然后直接通过 request path 喂给 shortlist
- 不需要先手写进主 request 里

当前新增模板：

- `financial-analysis/skills/month-end-shortlist/examples/month-end-shortlist-x-discovery-request.template.json`

当前 path-based example 也已经跑过：

- `.tmp/x-discovery-path-example/result.json`
- `.tmp/x-discovery-path-example/report.md`

在这条 path-based 例子里：

- report 已真实出现：
  - `## 重点观察`
  - `## Event Board`
  - `## Chain Map`

当前 focused regression 已扩到：

- `46 passed`

## Discovery Lane Phase 2.0

开始进入你一开始点名要的“更真实的信号质量提升”本体：

1. 不再只保留散的 discovery rows
2. 现在会把同一只票来自不同来源的信号合成一张 `event card`

当前 event card 已会合并：

- 官方预告 / 公告
- X 摘要
- 公司回应
- 量价验证

当前 event card 已输出这些关键字段：

- `primary_event_type`
- `source_count`
- `source_accounts`
- `event_state`
- `trading_usability`

当前多源 event card example 已验证：

- `.tmp/event-card-example/result.json`
- `.tmp/event-card-example/report.md`

在这个 example 中：

- `000988.SZ` 华工科技
  - 已被合成为单一 event card
  - report 中已真实出现：
    - `## Event Cards`
    - `source_count`
    - `source_accounts`
    - `event_state`
    - `trading_usability`

当前 focused regression 已扩到：

- `50 passed`

## Discovery Lane Phase 2.1

又继续把 event card 从“能合并”往“更像交易员面板”推进了一层：

1. 每张 event card 现在新增：
   - `priority_score`
   - `why_now`

2. `priority_score` 会综合：
   - 事件状态
   - 交易可用性
   - 市场验证强度
   - 来源层级

3. `why_now` 会压成一行可读判断：
   - 当前为什么值得优先看
   - 是官方确认、还是公司回应确认、还是只是强验证
   - 当前有哪些关键账号或摘要源参与了这张卡

当前多源 example 复跑后，report 里已真实出现：

- `priority_score`
- `why_now`

当前 focused regression 已扩到：

- `51 passed`

## Discovery Lane Phase 2.2

- `x_discovery_request_path` 现在不仅支持单个 subject request，也支持 batch-style X payload。只要请求里带有 `subject_registry / shared_request / subject_overrides_by_handle / selected_handles` 这些 batch 关键词，wrapper 就会自动走 `run_x_stock_picker_style_batch(...)`，不需要再先手工跑一层 batch 结果。
- 新增了真实 X multi-source fixture/template，基于这 3 条内容：
  - `twikejin/status/2044729369474863349`
  - `LinQingV/status/2044710801597223339`
  - `tuolaji2024/status/2042465436810559801`
- `earnings_momentum_discovery.py` 现在给 event card 增加了更像交易员面板的证据层：
  - `evidence_mix`
  - `source_urls`
  - `key_evidence`
  - `market_signal_summary`
  - `chain_path_summary`
- `build_x_style_discovery_candidates(...)` 也不再只保留 thesis excerpt，而是会尽量带上 `published_at / evidence_excerpt / source_kind / status_url`，让后面的 event card 能把真实证据摘要直接展示出来。
- `## Event Cards` 报告段现在已经会把这些字段真实渲染出来，不再只是 `priority_score / why_now / source_count` 这一层结构化结果。
- discovery/reporting focused regression：`42 passed`

## Discovery Lane Phase 2.3

- 针对真实 report 的可读性反馈，又补了一层“判断先行”的解释层，不再只是把零散证据平铺出来。
- event card 现在新增：
  - `event_phase`
  - `expectation_verdict`
  - `headline_metrics`
  - `community_reaction_summary`
  - `leaders / peer_tier_1 / peer_tier_2`
- 其中 `event_phase` 用来区分：
  - `正式结果`
  - `官方预告`
  - `预期交易`
  - `传闻博弈`
- `expectation_verdict` 会先给交易判断，例如：
  - `超预期`
  - `符合预期`
  - `不及预期`
  - `市场押注超预期`
  - `暂无一致预期`
- `Event Board` 现在不再只是 raw rows，而是会写成：
  - 阶段
  - 预期判断
  - 关键数据
  - 社区反应
  - 市场验证
- `Chain Map` 也不再只显示命中的单只票，而是会结合 batch request 里的 `subject_registry.logic_basket_rules` 抽出：
  - `当前事件锚点`
  - `龙头/核心`
  - `一线`
  - `二线`
- 新的 real-X example 已重生成到：
  - `.tmp/real-x-event-card-example/report.md`
  - `.tmp/real-x-event-card-example/result.json`
- 当前 discovery/reporting focused regression：`43 passed`
