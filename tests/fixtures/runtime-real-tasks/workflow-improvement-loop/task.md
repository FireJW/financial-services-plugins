As of 2026-04-01, turn the provided offline workflow-improvement pack into a
keep-or-rollback judgment for a repeatable runtime-reliability loop.

Keep `fixed task profile`, `sample set`, `current baseline`, `score
dimensions`, `candidate change`, `verification result`, and final `keep /
rollback` decision separate. Do not keep a change just because it sounds
better.
